const { readdirSync } = require('fs');

module.exports = {
    name: 'Belladone-1-StartRandomEvent',
    description: 'Événement aléatoire avec Belladone.',
    async execute(client, message) {
        console.log('test')
    }
}